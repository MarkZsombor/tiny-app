<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/_head.ejs %>
</head>

<header>
    <% include ../partials/_header.ejs %>
</header>


<body>

<p><%= 'The link ' + urls[shortURL].long + ' has been shortened to http://localhost:8080/u/' + shortURL %></p>


<% if(urls[shortURL].userID === user_id.id) { %>

    <form action="/urls/<%= shortURL %>" method="POST">
      <label for="longURL">Update the URL:</label>
      <input id="longURL" type="text" name="longURL" placeholder="http://" style="width: 300px">
      <input type="submit" value="Update">
    </form>

<% } %>

<!-- <% if(!user_id) { %>

  <p>You are not the creator of this link. You will be unable to modify it.</p>

<% } %>
 -->
</body>
</html>